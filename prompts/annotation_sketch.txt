Image to sketch
Convert the image to display a sketch map.
Turn this image into a sketch map that outlines the scene's major components.
Create a detailed sketch map by tracing the visual elements in the image to highlight its contours.
Generate a sketch map that captures the essential outlines and shapes of the image in a sketch-like style.
Transform the image into a sketch map
Make a sketch map that simplifies the image into basic outlines and key features, like a hand-drawn sketch.
Process the image to create a sketch map, showing a simplified version focused on its linear details.